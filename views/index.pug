extends layout

block content
  h1 CHAD dataset downloader
  p This is a crawler to download 
    a(href="https://github.com/amanteur/CHAD") CHAD dataset
    |  using 
    a(href="https://instances.hyper.lol/") multiple cobalt instances.
    |  The dataset has about 36,000 YouTube songs.
  h2 Download status
  p Total: 
    span= count.total
    | , Downloaded: 
    span= count.success
    | , Failed: 
    span= count.failed
  h2 Instance status
  table
    tr
      th API
      th Status
      th Success
      th Failed
      th Quota
    for inst in instances
      tr
        td= inst.endpoint
        td(title=inst.status)= inst.status === 'up' ? '✅' : (inst.status === 'deleted' ? '🗑️' : '❌')
        td= inst.success
        td= inst.fail
        td= inst.quota
